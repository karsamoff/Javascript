var nom;    /* a la variable se le da el atributo nom con el fin de utilizar la misma funcion con el mismo nombre (nom)*/
nom = prompt ("Write Your name"/* message of the alert */,"NAME/* the text in the page */");/* el atributo nom es igual a la function prompt que es para añadir un texto */
alert ("Mucho gusto " + nom + ' es un placer');/* respuesta con el texto agregado */